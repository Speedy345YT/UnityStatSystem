StatBase - Stores all parent classes. Like StatModifierBase, and IStat
FloatStat - Implements IStat<float>, and uses StatModifier<float> to add moodifiers.
VectorStat - Implements IStat<Vector3>, and uses StatModifier<Vector3> to add modifiers.
